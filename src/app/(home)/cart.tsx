import { CartPage } from "@/features/cart";
import { View } from "react-native";


export default function CartScreen() {
    return (
        <View style={{ flex: 1 }}>
            <CartPage />
        </View>
    );
}