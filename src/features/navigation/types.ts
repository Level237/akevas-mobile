export type Category = {
    id: string;
    label: string;
    url: string;
};

export type ExploreProduct = {
    id: string;
    title: string;
    price: number;
    imageUrl: any;
    isFavorite: boolean;
};
